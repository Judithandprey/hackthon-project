# Set these in your terminal locally or in Render's Environment settings.
# Generate secrets with: ruby -rsecurerandom -e 'puts SecureRandom.hex(32)'
ORGANIZER_INVITE_CODE=
# Production only (automatically supplied by render.yaml):
DATABASE_URL=
SECRET_KEY_BASE=
